@model VendorFormViewModel
@{
    ViewData["Title"] = "Edit Vendor";
}
<div class="mx-auto max-w-3xl">
    <div class="mb-6">
        <h1 class="text-[26px] font-semibold tracking-tight text-stone-900">Edit Vendor</h1>
        <p class="mt-1 text-sm text-stone-400">Update the vendor's details.</p>
    </div>
    <partial name="_VendorForm" model="Model" />
</div>
