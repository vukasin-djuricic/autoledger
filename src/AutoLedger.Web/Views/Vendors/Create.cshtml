@model VendorFormViewModel
@{
    ViewData["Title"] = "New Vendor";
}
<div class="mx-auto max-w-3xl">
    <div class="mb-6">
        <h1 class="text-[26px] font-semibold tracking-tight text-stone-900">New Vendor</h1>
        <p class="mt-1 text-sm text-stone-400">Add a counterparty entries can be linked to.</p>
    </div>
    <partial name="_VendorForm" model="Model" />
</div>
