@model AccountFormViewModel
@{
    ViewData["Title"] = "Edit Account";
}
<div class="mx-auto max-w-3xl">
    <div class="mb-6">
        <h1 class="text-[26px] font-semibold tracking-tight text-stone-900">Edit Account</h1>
        <p class="mt-1 text-sm text-stone-400">The code and type are fixed once created.</p>
    </div>
    <partial name="_AccountForm" model="Model" />
</div>
