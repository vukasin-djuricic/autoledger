@model AccountFormViewModel
@{
    ViewData["Title"] = "New Account";
}
<div class="mx-auto max-w-3xl">
    <div class="mb-6">
        <h1 class="text-[26px] font-semibold tracking-tight text-stone-900">New Account</h1>
        <p class="mt-1 text-sm text-stone-400">Add a line to the chart of accounts.</p>
    </div>
    <partial name="_AccountForm" model="Model" />
</div>
